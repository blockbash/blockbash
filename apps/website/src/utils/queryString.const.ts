enum FilterOptions {
  AND = "AND",
  OR = "OR",
}

enum QueryStringKey {
  tutorialCategories = "tutorialCategories",
  tutorialFilterMode = "tutorialFilterMode",
  tutorialName = "tutorialName",
  tutorialTypes = "tutorialTypes",
}

export { FilterOptions, QueryStringKey }
